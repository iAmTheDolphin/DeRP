function rec using (x, y, z) {

	if x > 0 {
		print("" + x + " " + y + " " + z)
		rec(x - 1, y, z + 1)
	}
	otherwise {
		print("base case reached. Exiting.")
	}

}

function main using () {
	rec(10, 5, 0)
}